# iOS placeholder

iOS is intentionally not generated or released in the Android MVP. Keep shared
Flutter features platform-neutral; create the native iOS runner only when the
iOS milestone is authorized and signing requirements are known.
