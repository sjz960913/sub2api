name: sub2api_mobile
description: Android-first Sub2API client with Codex PC collaboration.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  dio: ^5.11.0
  flutter_riverpod: ^3.4.1
  flutter_secure_storage: ^10.3.1
  go_router: ^17.3.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  generate: true
  uses-material-design: true
